# Database environment variables
POSTGRES_DB=rustbot
POSTGRES_USER=rustbot
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_PORT=5432

# pgAdmin configuration
PGADMIN_EMAIL=admin@rustbot.local
PGADMIN_PASSWORD=your-admin-password-here

# Replace the passwords above with secure values
# Make sure to update the pgpass file as well when changing passwords

PGADMIN_DEFAULT_EMAIL=admin@rustbot.local
PGADMIN_DEFAULT_PASSWORD=admin